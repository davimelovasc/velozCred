<%= form_for :archive, url: save_archive_path do |f| %>
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Atualização de arquivos do Access</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>É necessário selecionar os dois arquivos, e que os mesmos estejam com os seguintes nomes: "Cadastro.accdb" e "Producao.accdb"</p>

        <div class="form-group">
          <%= f.label :register, "Cadastro.accdb: " %>
          <%= f.file_field :register, class: 'form-control'%>
        </div>

        <div class="form-group">
          <%= f.label :total_production, "Producao.accd: " %>
          <%= f.file_field :total_production, class: 'form-control'%>
        </div>

        <div class="form-group">
          <input type="checkbox" id="update_total_prod" style="display: inline-block" name="update_tp" value="true"> <label for="update_total_prod" style="display: inline-block">Adicionar e atualizar produção total</label>
        </div>

        <div class="form-group none" id="year_months_div">
          <% @ym.each.with_index(1) do |y, i| %>
            <input type="checkbox" id="<%= y.year_month %>" style="display: inline-block;" name="year_months[]" value="<%=y.year_month%>"> <label for="<%= y.year_month %>" style="display: inline-block;margin-right: 15px;" class="year_month"><%= y.year_month %></label>
            <% if i % 3 == 0%>
            <br/>
            <% end %>
          <% end %>
        </div>

      </div>
      <div class="modal-footer">
        <%= f.submit 'Atualizar', class: 'btn btn-primary' %>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
      </div>
    </div>
  </div>
</div>

<% end %>
